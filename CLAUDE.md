# CCG 词典求解器 — 项目指令

> 本文件是本项目的编码代理指令（`CLAUDE.md`）。每次开工前先通读，尤其是 §2 里程碑、§5 陷阱、§7 语料、§11 协作方式。
> M1 裁定与 M2 工作书见 `docs/m1-conclusion-m2-plan.md`，域过滤增补见 `docs/m2-addendum-3-domain-filtering.md`，退化引理见 `docs/m2-addendum-4-degeneracy-lemma.md`；修订均已同步进本文件。

---

## 0. 角色

你是这个项目的实现者。项目目标是：**从无标注的句子集合中，把每个词的 CCG 范畴当作未知数解出来。**

核心直觉：每个合法句子等价于一条类型判断（词序列归约为 S），因此一篇语料是一个关于词范畴的方程组，可以用消元、传播、回溯的方式求解。

不要把它做成一个统计归纳系统。这是一个**约束求解**项目，统计量只在目标函数和启发式里出现。

---

## 1. 问题的精确形式

- 给每个词型 $w$ 一个类型变量 $x_w$，取值域是 CCG 范畴集合。
- 范畴文法：`Cat ::= Atom | Cat / Cat | Cat \ Cat | Var`，原子范畴取自有限集（初期固定为 `S`、`N`、`NP`、`PP`、`conj`；`S` 带特征 `[dcl] [q] [wq]`）。
- 组合规则：M1–M3 **默认只开**前向应用 `>`、后向应用 `<`。前向/后向组合 `>B` `<B` 已实现但默认关闭，M4 再开（开之前记录 golden 计数，开之后对比）。**类型提升 `T` 在 M0–M3 完全关掉**（无限制的 T 让 CKY 不终止，M4 之后再议）。**不要**一开始就开广义组合和替换规则。
- 一个句子 $w_1 \dots w_n$ 的约束是：**存在**一种推导，使 $x_{w_1} \dots x_{w_n}$ 归约到 `S[f]`，其中 `[f]` 由句末标点决定。
- 注意这个"存在"：单句不是一个方程，而是若干方程组的**析取**。推导一旦固定，剩下的是纯一阶合一，多项式可解。所有难度都来自这个析取。

假设类分层：
- **刚性（rigid）**：每个词型恰好一个范畴。初期只做这一层。
- **k 值**：每个词型至多 k 个范畴，k ≤ 3。只在刚性无解时逐步放宽。

---

## 2. 里程碑

按顺序做，每个里程碑必须有可运行的验证才进入下一个。各阶段用什么语料见 §7.2。

| 阶段 | 内容 | 完成判据 |
|---|---|---|
| **M0** | 范畴表示 + 一阶合一 + 带撤销的并查集 | 单元测试：`S/X` 与 `Y/NP` 合一得 `S/NP`；`S/NP` 与 `S\NP` 合一失败；occurs check 生效 |
| **M1** | 单句 CKY（格子存含变量表达式），枚举存活推导，按 σ 去重 | M1-a 金标准 σ ∈ D；M1-b D 可复现（含顺序）；M1-c 固定词典时 σ 折叠正确；M1-d 6 个自由未知数 < 5 s；M1-e 单调性可执行断言。**不承诺唯一性**（见 §5.4） |
| **M2** | 跨句传播：backbone 提取 + 优先队列消元 | ~30 句人造语料（见 §7.2，含 VP 修饰语和最小对）。M2-a backbone 提取正确；M2-b 出现连锁单位子句；M2-c 队列顺序可复现且短句先出；M2-d **压缩率报告**分两个数：只开 backbone 时 Σ log\|D_s\| 相比逐句独立求解的下降，以及再开域过滤后的额外下降（预期后者显著更大；若不是，问题在语料多样性）；M2-e 单调性仍成立。**不承诺唯一解** |
| **M3** | 以目标函数为主的分支定界（外循环 L-迭代加深；传播降为机会性剪枝，只在非退化句上跑）+ 回溯 + 冲突学习 | CHILDES（或 ATIS）中长度 ≤ 8 的句子子集能求出可行解。**不要直接上 CCGbank**（原因见 §7.1） |
| **M4** | 目标函数 + 拆词（k 值放宽） | 兼类词（如 `run`、`that`）被正确拆分，词典不爆炸 |
| **M5** | 共享图表（n-gram 键控）+ 性能优化 | 千句级语料（Tatoeba 英语短句）可在分钟级求解 |

---

## 3. 数据结构规格

### 3.1 范畴
- 代数数据类型，**hash-cons 成 DAG**，结构相等即指针相等。
- 变量以整数 id 表示，全局唯一。
- 复杂度度量：论元个数 + 嵌套深度。**硬上限：论元数 ≤ 4**（CCGbank 中超过的极少），超限的候选直接剪掉，否则搜索空间无限。

### 3.2 全局状态（"增广矩阵"）
- 带撤销记录（trail）的并查集，支持合同闭包：若 `X = Y`，则 `S/X = S/Y` 自动成立，反之两复合项相等则子项对应合并。
- 必须有 occurs check：范畴不允许循环项。
- 每次 `union` 记录到 trail，决策点记录 trail 位置，回溯时逐条撤销。
- **域表**（M2，永久设计）：`domain: Var → 模式集合`。域是含变量的模式的有限集合（"变量是其中某个模式的实例"），`None` = 复杂度界 L（§4.6）下的全体候选，**不显式枚举**（按论元数+深度，L=2 在 4 斜杠内截断已有 136,512 个候选）。交集 = 两两 mgu 去重并按包含关系极小化；并查集合并时两域取交集；变量绑定到项时域过滤为相容模式。**域变空即冲突**，比合一失败更早。域的改动同样记 trail。全局复杂度界对所有范畴变量生效（模式内变量也受限）。

### 3.3 语料因子图
- 二分图：一侧词型（变量），一侧句子（因子）。
- 用途一：算每个句子的"剩余未知数个数"，驱动消元顺序。
- 用途二：全局状态变化时，**只重算共享该变量的句子**，不做全局失效。
- 用途三：连通分量可以独立求解。

### 3.4 共享图表（M5）
- CKY 格子按 **n-gram 而非（句子, 位置）** 键控。刚性假设下，一个 span 能得到的范畴集合只取决于它覆盖的词序列。
- n-gram 长度上限 8，超过的不共享。
- 每个格子记录它依赖哪些变量；变量合并时按依赖失效重算。
- 拆词会破坏"按词型键控"的前提：被拆的词改为 token 绑定键控，或干脆退出共享。

---

## 4. 算法组件

### 4.1 局部求解器（一句话 = 一行方程）
输入：句子 + 当前全局状态。跑 CKY，格子里放含变量的范畴表达式。每条完整推导产生一组等式，逐一试与全局状态合一，得到存活推导集 `D`。三种输出：

- `|D| = 0` → **冲突**，交给回溯层。
- `|D| = 1` → **单位子句**，等式并入全局状态，句子出队。
- `|D| > 1` → 计算所有存活推导**共同蕴含的等式**（backbone），并入全局；句子留在队列。

局部求解器返回 `(D, backbone, projected_domains)`。第三项是把 `D` 投影到本句每个词型变量上得到的候选集；全局侧与域表取交集并记录是否收缩。精度层级：backbone（等式交集）⊂ 域过滤（逐变量域交集）⊂ 句对 join（σ 元组级关系交）⊂ 联合解集。

**§5.5 的退化引理限定了以上所有基于投影的传播的适用范围：只对 $n \ge L+2$ 的非退化句子有效，退化句子上 backbone、域过滤、关系投影全部恒为零。** 句对 join（对 `|D_s| < join_threshold` 的非退化句子保留完整 σ 集，共享变量时做元组级 mgu 关系交）有条件采纳，只在非退化句子上做。

**推导的粒度按 σ 去重。** 一条推导蕴含的范畴指派 σ = `trail[mark:]` 限制到词型变量上（局部新变量按首次出现顺序规范重命名）。两条推导 σ 相同就算一条。这个等价关系精确地就是"虚假歧义"的定义：不做 Eisner 正规形，不会因为正规形定义和规则集不匹配而漏折叠或误折叠。

~~第三种情况是整个系统的性能关键。~~ **这个前提已被 M2 实测证伪**（压缩率 0.0%，见 `docs/m2-addendum-4`），且是第二次出现同一信号（M1：把 `sees` 从 14 压到 2 的是复杂度项，不是跨句交集）。结论：**信息不在一致性里，在目标函数里。** 传播的角色从"主力"降为"剪枝的一个来源"：在非退化句子上跑，退化句子上跳过，不再在传播层投入。M3 相应重组为以目标函数为主的分支定界（外循环 L-迭代加深，主体按代价的分支定界，传播只是机会性检查）。

### 4.2 消元顺序
~~优先队列按 `(未知变量数, 存活推导数, 句长)` 升序，短句先解。~~ **方向反了**：对过滤而言短句最退化（§5.5）。改为：非退化句（$n-1 > L$）优先，其中按 $n$ 降序；退化句排在其后。注意这不推翻 §7 的"短句信息密度高"：那讲的是语料层面的可解性，这里讲的是单句能否提供过滤，两个指标不同。理想语料是词型少、句子多、但句长略超过 L+1。

**重入队条件**：共享变量的**域发生收缩**（不只是被合并）。这把单遍扫描变成不动点迭代（arc consistency），是 M2 的关键。域过滤怕域大，§4.6 的迭代加深恰好治它：L=1 候选个位数，L=2 几十个，两个机制配套使用。

语料层面还有一层消元顺序：**频次截断**（§7.3）。先只为高频词设变量，求解到收敛；再把已解出的范畴当作已知量，逐轮放宽阈值去解次高频词。

额外的高优先级输入源：**最小对**。两句只差一个词（`the cat sleeps` / `the dog sleeps`），用前缀树 ∩ 后缀树找出。这类信息不需要任何推导，是代价最低的方程。注意：单个上下文的可替换性**不蕴含**同范畴，多个上下文同时可替换才够强 —— 所以作为**高置信度候选合并**，不作硬约束。

### 4.3 决策与回溯
队列停滞（无单位子句）时：
1. 选存活推导数最少的句子。
2. 按 MDL 代价排序其推导，取第一个作为决策，记录 trail 位置。
3. 继续传播；冲突则回退，换下一条推导。
4. **子句学习**：记录导致冲突的决策集合，避免重复走同样死路。

这就是 CDCL(T)，T = EUF（未解释函数的等式理论）。

### 4.4 拆词（打破刚性）
**只有当某句在所有决策分支下都冲突时才拆词。** 顺序不能反：先回溯穷尽推导空间，否则会把"推导选错"误判为"词有歧义"，词典迅速膨胀。

- 拆哪个词：取参与冲突次数最多的词型（跨句统计）。
- 怎么拆：给该词型的部分 token 分配新变量，token 级重新绑定。
- 代价计入目标函数；k 设硬上限 3。
- 拆词后，含该词的句子全部重新入队。
- 经验预期：被拆的几乎总是功能词和高频动词，符合 Zipf 分布。

### 4.5 目标函数
可行解通常有很多，**必须**有目标函数选解。加权和：

```
cost = α₁ · |词典条目数|
     + α₂ · |词典中出现的不同范畴数|        ← 奖励范畴在词之间的重用
     + β  · Σ 范畴复杂度
     + γ  · Σ_句子 log(去重后 σ 数)
```

`α₂` 项是 §5.4 揭示的真正消歧机制：让 `sleeps` 和 `sees the dog` 落入同一范畴的词典更好。

**β 项是全局项，不是逐词规则。** 若实现成逐词贪心偏好，每个词都会想当原子范畴，结果没有任何句子可推导。代码里要有注释挡住这种误读。

γ 项最重要，它压制**过度泛化**：一个过宽的语法（极端情况：所有句末词取 `S`，其余取 `S/S`）词典极小、能推导一切，但每句话会有几十种推导。这一项让它的总代价反而更高。

理论上正确的判据是"生成的语言最小"（Kanazawa），但语言大小算不出来，上式是它的可计算代理。

优化策略：先求可行解，再做局部搜索 —— 尝试合并两个等价类，检查是否所有句子仍可推导且推导总数不增。

### 4.6 复杂度上的迭代加深
把 `arity ≤ 4 / depth ≤ 3` 的硬上限之内再套一个递增的复杂度界 `L`，作为求解的外循环：

```
best = None
for L in 0, 1, 2, ...:
    sol = 求解(复杂度界 L)
    if sol and (best is None or cost(sol) < cost(best)):
        best = sol; stall = 0
    elif sol:
        stall += 1
        if stall >= 2: break
return best
```

这不是启发式，是完备搜索的一种顺序。它自动实现"种子词"：`L=0` 只有原子范畴，`cat`/`dog` 先定；`L=1` 引入 `NP/N`；`L=2` 及物动词才进入解空间。**"若有解则返回"是错的停止条件**：实测最小可行 L = 2，金标准需要 3（副词 arity 2 + depth 1）。最小可行 L 只作为下界报告指标，不作为停止条件，也不可当作金标准复杂度的估计。`State(complexity_bound=L)` 就是这个 L（对所有范畴变量生效）。

---

## 5. 必须知道的陷阱

### 5.1 单调性只在固定假设类内成立
**命题**：若 `C ⊆ C′`，则 `Sol(C′) ⊆ Sol(C)`，故 C 下在所有解上成立的等价关系，在 C′ 下仍成立。

这条性质**在三处失效**，实现时要清楚自己站在哪一侧：
1. **拆词**改变了假设类，`Sol` 变大，此前可证的等价可能消失。
2. **贪心选推导**是取 `Sol` 的一个子集，它做的合并并非所有解共有，新句子可能证伪，必须能撤销 —— 这就是为什么 trail 是必需的。
3. **argmin 选解**对约束集不单调：新句子可能让原本最省的词典失去资格。Kanazawa 保证极限收敛，不保证路径单调。

只有 backbone 传播（所有存活推导共同蕴含的等式）享有严格单调性。

### 5.2 正例学习的根本困境
闭合世界假设只提供正例。更宽的语法能推导语料每一句，也能推导语料外一堆句子，而后者永远不会作为反例出现。加语料不能纠正过度泛化，只有目标函数能。

### 5.3 Zipf 尾部
只出现一两次的词约束不足，会落到与上下文一致的最一般范畴上。这既不是错误合并也不是过度泛化，是数据稀疏。不要试图在求解阶段修复，标记为低置信度即可。语料层面的对策是频次截断（§7.3）。

### 5.4 解的对称性与不可识别性
与 §5.2 分开归类。`sees` 的两个最小解 `(S[dcl]\NP)/NP` 与 `(S[dcl]/NP)\NP` 之间存在一个**自同构**（翻转及物动词的论元顺序，其余不动），两个词典生成的字符串集完全相同（弱等价）。**任何标量目标函数在自同构下取值相同，全部无效**，不要靠调权重解决。

破对称只有两条路：注入不对称的先验（obliqueness 排序、主语最后合并；默认不采用，采用必须显式标注为注入的语言学约定），或引入打破对称的数据：VP 修饰语、VP 并列、助动词都让 `sees the dog` 成为与 `sleeps` 同范畴的 `S[dcl]\NP` 成分。这个歧义是语料过滤的产物，不是 CCG 的固有缺陷。

**结论：唯一性不来自单句方程，不来自跨句交集，不来自任何标量复杂度指标，而来自范畴在不同词之间的重用。** 后续每个设计决定都要放到这句话下面检验。

### 5.5 退化推导（纯论元退化引理）
只开 `>` `<` 时，对句子 $w_1 \dots w_n$ 的任意位置 $k$，"$w_k$ 是整句的函子、其余 $n-1$ 个词全是纯论元"永远是一条合法推导，其范畴 arity $= n-1$，depth $= 0$，复杂度 $= n-1$。若 $n-1 \le L$，则 $D_s$ 里存在除 $w_k$ 外所有词都绑定到互不相同自由变量的 σ。推论：(1) 逐变量域过滤在这类句子上恒为零；(2) $k$ 任取，任意真子集的关系投影同样不受约束；(3) 基于投影的过滤生效的必要条件是 $n \ge L+2$。

这是 CCG 词典求解区别于一般 CSP 的结构性特征：**约束的强度取决于 $L$ 与句长的相对关系，而非语料规模或多样性。** 增补 3 中"域不收缩可能是语料重复所致"的判断作废。副作用：退化解在复杂度上极其昂贵（arity $n-1$），只有代价函数能排除它们，一致性检查不能——这就是目标函数必须承担主要消歧责任的原因。

---

## 6. 标点处理

参考 CCGbank 的三类处理：
- 句末标点：吸收规则 `S . → S`。信息量在于**决定 S 的特征**（`?` → `S[q]`，`.` → `S[dcl]`），这会把疑问句和陈述句的动词范畴区分开。
- 并列逗号：`conj` 或 `(X\X)/X`，与 `and` 同类。
- 插入语逗号：类型变换，如 `NP , → S/S`。

**逗号真正的价值不是多几条方程，而是约束推导。** 逗号几乎总落在成分边界上，相当于免费的部分括号化，能大幅削减 CKY 的推导枚举量。实现为**软约束**：跨越逗号的组合步骤付额外代价，而非硬禁止。

---

## 7. 语料选择与预处理

### 7.1 方程数 / 未知数比

刚性假设下，未知数 = 词型数，方程 = 句子数。而且每个句子还是析取式，约束比等式弱得多，所以需要的比值远大于 1，经验上要 **5:1 起步**。按这个标准看常见语料：

| 语料 | 句数 | 词型 | 比值 | 均长 | 判断 |
|---|---|---|---|---|---|
| WSJ / CCGbank | ~48k | ~44k | ~1:1 | 24 | 直接上必然失败 |
| CHILDES (Eve/Adam) | ~20k | ~3k | ~7:1 | 4–6 | 理想 |
| ATIS | ~5k | ~1k | ~5:1 | 11 | 好，但领域极窄 |
| GeoQuery-880 | 880 | ~280 | ~3:1 | 8 | 太小，只够调试 |
| Tatoeba 英语 | 百万级 | ~40k | 高 | 6–8 | 规模阶段的主力 |
| PMB gold 英语 | 数千 | 小 | 高 | 短 | 有金标准，做评测 |

比值之外，**句长同样关键**。句长每加一个词，CKY 推导数近似按 Catalan 数增长，而约束只增加一条。短句是"稀疏方程"，信息密度高得多。CHILDES 的儿向语言平均四到六个词，既短又高度重复，这也是无监督文法归纳传统上偏爱它的原因。

### 7.2 分阶段语料

- **M0–M2：人造语料。** 从一部已知的小型 CCG 词典（二三十个条目）正向生成句子。这样有真正的 ground truth，验证的是求解器本身是否正确，而不是语言学假设。**这一步不能省**：后面所有失败都需要先排除"求解器有 bug"这一种可能。M2 语料要求：含 VP 修饰语、含最小对、**同一个动词出现在结构不同的宾语上下文里**（`the dog` / `dogs` / `the big dog`；这服务于目标函数的范畴重用项，不服务于域过滤）、**$n \ge L+2$ 的非退化句至少占三分之一**（§5.5），长度 ≤ 8，只用 `>` `<`。
- **M3：CHILDES 或 ATIS。** CHILDES 用 NLTK 自带的语料读取器，取 Eve 或 Adam 的成人话语部分。选 CHILDES 的理由是句长分布好且免费；选 ATIS 的理由是领域封闭、句式高度重复，方程之间的耦合强。GeoQuery-880 太小，只够调试。
- **M4–M5 评测：PMB。** pmb.let.rug.nl，免费下载，带 CCG 范畴标注，句子多来自 Tatoeba 因此很短。注意它的 CCG 层大部分由 EasyCCG 自动产生，只有 gold 部分经过人工校对，**只拿 gold 分区做准确率评测**。CCGbank 本身是 LDC 授权的付费资源（LDC2005T13）：拿不到，PMB 是最好的替代；拿得到，也只用它的短句子集，不要全量。
- **M5 规模阶段：Tatoeba 英语。**

### 7.3 最重要的杠杆：频次截断

§7.1 的比值可以人为改善，办法是**只为高频词设变量**。设一个阈值（比如出现 ≥ 20 次），低频词统一绑定到一个通配变量，或直接跳过含它们的句子。WSJ 的词型数在 freq ≥ 20 下会从四万掉到五千左右，比值立刻从 1:1 变成 10:1。

这个做法不是妥协，而是符合问题结构的：真正想解出的是功能词和高频动词的范畴 —— the、of、is、that、and —— 它们构成语法的骨架，也恰好是约束最充分的那批。Zipf 尾部的词本来就约束不足，强行纳入只会污染方程组。

所以流程应该是：**先在高频核心上求解到收敛，再把已解出的范畴当作已知量，逐轮放宽阈值去解次高频词。** 这是消元顺序（§4.2）在语料层面的体现。

### 7.4 预处理约定

- 小写化。
- **不做词干还原**：形态变化在英语里恰好携带范畴信息（`sleeps` 与 `sleep` 范畴确实不同）。
- **绝对不用 subword 分词**：会彻底破坏"词型 = 变量"的前提。
- 句长先截到 ≤ 8，通过后再放宽。

### 7.5 关于语言的选择

如果考虑英语之外：中文没有形态变化，词型数增长慢，比值天然更好，而且有从宾州中文树库转换来的中文 CCGbank（Tse & Curran）可做评测。代价是分词成了前置依赖，分词错误会直接变成错误的未知数。形态丰富的语言（芬兰语、土耳其语）是最坏情况 —— 词型爆炸让比值崩溃，除非先做形态切分，但那又引入了新的未知量。

---

## 8. 评测协议

不要一上来用真实无标注语料。

1. 取带金标准 CCG 标注的语料（PMB 的 gold 分区；若能拿到 CCGbank，只用其短句子集），**抹掉范畴标注**，只留词序列。
2. 用系统重新求解。
3. 与金标准对比前，先做一次**原子范畴重命名匹配**（系统解出的 `A1` 可能对应金标准的 `NP`），用二分图最大匹配确定对应关系。
4. 报告：词型级范畴准确率、词典大小比、每句平均推导数。
5. 子集顺序：长度 ≤ 5 → ≤ 8 → ≤ 12 → 全集。

**M3 的及格线**：在 M3 语料（CHILDES / ATIS，长度 ≤ 8）上能恢复出 `NP/N`（限定词）、`(S\NP)/NP`（及物动词）、`S\NP`（不及物动词）这三个核心范畴。

---

## 9. 技术栈

- **主语言 Python。** 理由：这个项目最不确定的是设计（backbone 传播够不够强、MDL 权重怎么配、拆词判据是否稳定），需要反复改启发式并检视中间状态，改动速度远比运行速度重要。生态也压倒性（Z3 绑定、CCGbank 处理、评测、可视化）。
- 范畴用 `dataclass(frozen=True)` + 手写 hash-cons 表。
- **可选捷径**：M3 之前可以先把第 4.1–4.3 的逻辑编码进 Z3 验证可行性 —— 范畴作为 datatype，推导选择用布尔变量，MaxSMT 直接求最小词典。这能在写搜索引擎之前先确认约束建模是对的。
- 重写时机：M5 之后若瓶颈确为搜索速度，候选是 Rust（`egg` 提供 e-graph 与合同闭包，回溯搜索和并查集也合适）。Haskell 只在项目转向形式化验证时才是更好的选择。

工程约定：
- 包名 `ccg_solver`，源码在 `src/ccg_solver/`，测试在 `tests/`，按里程碑命名（`test_m0_*.py`）。
- 运行测试：`pytest`（`pyproject.toml` 已把 `src` 加进 `pythonpath`，无需安装）。

---

## 10. 非目标

明确不做的事，避免范围蔓延：
- 不做语义解析、不引入逻辑式。
- 不做神经网络、不做词向量。分布信息只用于生成候选合并，不用于打分。
- 不追求处理全部 CCGbank 现象（并列省略、长距离依存的完整分析）。
- 不做增量在线学习，批处理即可。
- M5 之前不优化性能。

---

## 11. 与我协作的方式

- 每个里程碑开始前，先写出该阶段的测试用例给我看，确认后再实现。
- 遇到设计歧义（例如目标函数权重、拆词判据）**停下来问**，不要自行选一个继续。
- 报告求解结果时，附上：解出的词典（按频次排序前 30 条）、未解出的句子数、平均存活推导数、触发拆词的词型列表。
- 代码里保留中间状态的 dump 接口，便于我在 notebook 里检查。
- 如果发现某个设计假设被数据证伪（例如刚性假设在 50 句内就崩溃），立刻报告，不要靠加 k 值硬撑过去。

---

## 附：核心概念速查

| 概念 | 在本项目中的含义 |
|---|---|
| 合一 (unification) | 找变量赋值使两个范畴表达式相同。给出最一般合一子，只做必要赋值。 |
| 合同闭包 | 已知 `X=Y` 时自动推出 `S/X = S/Y`。与合一方向相反：合一是求解，闭包是推论。 |
| backbone | 所有存活推导共同蕴含的等式。唯一享有单调性的传播。 |
| 刚性语法 | 每词型一个范畴。Kanazawa 证明其在 Gold 意义下可从字符串学习。 |
| 虚假歧义 | CCG 组合律导致的语义等价推导。不同于真歧义，应在推导计数时折叠。折叠判据：σ 相同（§4.1），不用正规形。 |
| MDL | 描述长度 = 词典编码 + 推导编码。后者负责惩罚过宽语法。 |
| 频次截断 | 只为出现次数 ≥ 阈值的词型设变量；先解高频核心，再逐轮放宽阈值。 |
| 退化句 | $n-1 \le L$ 的句子：任一词都可做整句函子，对所有基于投影的传播透明（§5.5）。 |
